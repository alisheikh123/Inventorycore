@model IEnumerable<Inventory_Management_Systems.Models.tblVoucher>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.voucherCode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.voucherDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.userId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.createdDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.voucher_Type)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TblInvoice)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.voucherCode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.voucherDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.userId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.createdDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.voucher_Type)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TblInvoice.CompanyName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.voucherId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.voucherId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.voucherId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
